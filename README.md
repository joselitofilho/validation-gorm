# validation-gorm

## Build

Build (first time only):
```bash
$ docker-compose build
```

Run containers:
```bash
$ docker-compose up -d
```

## Run

```bash
$ docker-compose exec app go run cmd/validation-gorm/main.go
```